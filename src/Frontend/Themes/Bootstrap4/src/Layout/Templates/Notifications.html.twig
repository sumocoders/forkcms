<!-- warning for people that don't have JS enabled -->
<noscript>
  {{ alerts.alert('danger', 'msg.EnableJavascript'|trans|raw, true) }}
</noscript>

<!-- Warning for people that still use IE7 or below -->
<!--[if lt IE 8 ]>
  {{ alerts.alert('danger', 'msg.OldBrowser'|trans, true, 'ie') }}
<![endif]-->

<a href="#main" class="muted hide">{$lblSkipToContent|ucfirst}</a>

{% if not cookieBarHide %}
  <div id="cookieBar" class="cookiebar alert alert-fullwidth">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <div class="container">
      <div class="row">
        <div class="col-sm-8">
          <strong>{{ 'lbl.Warning'|trans|capitalize }}:</strong> {{ 'msg.Cookies'|trans|raw }}
        </div>
        <div class="col-sm-4">
          <a href="#" id="cookieBarAgree" class="btn btn-primary btn-sm">{{ 'lbl.IAgree'|trans|capitalize }}</a>
          <a href="#" id="cookieBarDisagree" class="btn btn-primary btn-sm">{{ 'lbl.IDisagree'|trans|capitalize }}</a>
        </div>
      </div>
    </div>
  </div>
{% endif %}

